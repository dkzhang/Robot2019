# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `gitcode_go test -c`
*.test

# Output of the gitcode_go coverage tool, specifically when used with LiteIDE
*.out
